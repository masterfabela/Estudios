set serveroutput on
create or replace procedure libros as

--cursor cactores is select coda, noma from actores;

begin

/*for fa in cactores loop
end loop;*/

/*exception
    when no_data_found then
    dbms_output.put_line('mensaxe');*/
end;
/
show errors;
